version: "1.0"
linter: jetbrains/qodana-python:2022.3-eap
bootstrap: python -m venv venv && source venv/bin/activate && pip install -r requirements.txt --quiet
profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses
